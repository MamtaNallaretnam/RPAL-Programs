//Program to display palindrome numbers in a given rang

let rec rev (n, x) = n gr 0 -> rev (n/10, (x*10 + n - (n/10)*10)) | x
	within
		rec pal(a, b) = 
			b ls a -> '' |
			rev (a,0) eq a -> (pal(a+1,b), print(' '), print(a))|
				pal(a+1,b)						
in
pal(1, 10)
